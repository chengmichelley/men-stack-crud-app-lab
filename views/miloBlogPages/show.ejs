<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= miloBlog.name %></title>
</head>
<body>
    <%- include('../partials/navbar.ejs') %>
    <h1><%= miloBlog.name %></h1>
    <p><%= miloBlog.content %></p>
    <a href="/miloBlogs/<%= miloBlog._id %>/edit">
        <button> Edit <%= miloBlog.name %> </button>
    </a>
    <form action="/miloBlogs/<%= miloBlog._id %>?_method=DELETE" method="POST">
        <button type="submit" onclick="return confirm(`Are you sure you want to delete ${miloBlog.name}?`)">Delete <%= miloBlog.name %></button>
    </form>
    <button onclick="window.history.back()">Go Back</button>
</body>
</html>